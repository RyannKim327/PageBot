import { CommandProperties, json } from "../interfaces";

export default function commandControl(event: json, commands: CommandProperties[], admins: string[] | number[]) {
	let index = 0
	let done = false

	const execute = () => {
		let commands[index]

	}
}
